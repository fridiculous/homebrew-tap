# homebrew-tap

Homebrew tap for Fridiculous CLI tools.

Install Rostrum:

```bash
brew tap fridiculous/tap
brew install rostrum
```

Or in one command:

```bash
brew install fridiculous/tap/rostrum
```
